@model int

@{
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<div class="container-fluid">
    <div class="row">
        <div class="col-md-6 main">
            <h4>Импорт прошел успешно!!!</h4>
        </div>
        <div class="col-md-6 main">
            <a class="btn btn-default" href="@Url.Action("DefaultSnapshot", "Admin", new { snapshotId = Model})">
                 Пометить снимок как основной
            </a>
        </div>
    </div>
</div>

